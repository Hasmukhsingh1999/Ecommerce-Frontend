"use client";
import React, { useEffect, useState } from "react";

const Sale = () => {

  return (
    <div
      className={`bg-black min-h-[3rem] text-center text-white flex items-center justify-center `}
    >
      Sale
    </div>
  );
};

export default Sale;
